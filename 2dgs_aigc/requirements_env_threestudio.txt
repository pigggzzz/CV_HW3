# env_threestudio：Python 3.10 + CUDA 12.x
# 用途：Object B 文本到 3D（threestudio + SDS）
#
# 创建环境示例：
#   conda create -n env_threestudio python=3.10 -y
#   conda activate env_threestudio
#   pip install -r 2dgs_aigc/requirements_env_threestudio.txt
#   # 再按 threestudio 官方文档安装其依赖与 diffusers 等

# --- 本工程封装层 ---
PyYAML>=6.0.1
wandb>=0.17.0
tqdm>=4.66.0
numpy>=1.26.0
packaging>=24.0

# --- threestudio 核心依赖（版本请与所用 threestudio 分支 README 对齐）---
# 以下为常见依赖占位，安装 threestudio 时以其 requirements 为准：
# torch>=2.1.0
# torchvision>=0.16.0
# diffusers>=0.25.0
# transformers>=4.36.0
# accelerate>=0.25.0
# einops>=0.7.0
# omegaconf>=2.3.0
# pytorch-lightning>=2.1.0
# trimesh>=4.0.0
# xatlas>=0.0.9
# nerfacc>=0.5.0
